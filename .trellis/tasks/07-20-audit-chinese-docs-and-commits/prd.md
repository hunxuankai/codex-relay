# 复核内部文档中文化任务完成度

## 目标

独立复核已归档任务 `chinese-docs-and-commits` 的真实完成度，并在用户确认后修复审计发现的全部剩余缺口。由于原任务经历多次中断，本任务不直接采信原任务中的验收勾选或历史成功结论，而是从当前 Git 状态、提交差异、受管文件和可重复命令重新建立证据，再以持久回归测试驱动缺口修复。

## 已确认事实

- 原任务已归档到 `.trellis/tasks/archive/2026-07/07-20-chinese-docs-and-commits/`，`task.json` 标记为 `completed`。
- 原任务记录了三笔工作提交：`e96edf3`、`da1be7a`、`7b391ea`；随后有归档提交 `484eee5` 和会话日志提交 `d69feb1`。
- 原任务目标覆盖 `.agents`、`.codex`、`.trellis` 的自然语言中文化、长期中文文档规则、Git 提交信息规则和 Trellis 自动提交主题。
- 当前 `master` 工作树在创建本审计任务前为干净状态。
- 本审计任务由 `task.py create` 创建时，默认 `prd.md` 仍实际生成英文标题和说明；当前源头位于 `.trellis/scripts/common/task_store.py:173`。
- 用户于 2026-07-21 明确要求完成审计发现的全部剩余工作，并授权 AI 自行提交。
- 并行任务完成后，真实提交 `2acedd7 chore: 记录会话日志` 错误纳入本审计任务的 5 个材料文件。临时运行时复现确认：归档后的会话文件被清除时，`add_session.py` 会把唯一剩余的其他会话任务解析为 `session-fallback`。
- 修复实现、持久测试和 workflow spec 已提交为 `f86ddc5 fix(trellis): 中文化任务脚手架并隔离会话提交`，该提交只包含计划中的 6 个交付文件。
- 项目安全规则禁止开发任务读取真实 `%USERPROFILE%\.codex` 与 `%LOCALAPPDATA%\CodexRelay`。因此不调用会读取真实 `~/.codex/sessions` 的 `trellis mem`；多次中断的历史以归档任务材料、Git 历史和当前会话记录复核。

## 需求

1. 逐条建立原任务需求与验收标准的审计矩阵，结论只能为“通过”“部分通过”“未通过”或“无法验证”，每项附当前证据。
2. 复核 `.agents/**/*.md`、`.trellis/**/*.md` 和 `.codex` 受管配置/Agent 文件中的解释性自然语言，区分应翻译英文与命令、路径、协议字段、平台名、代码标识符等允许保留项。
3. 复核 Markdown 围栏、Skill frontmatter、相对链接、TOML/YAML/JSON/Python 结构，以及工作流解析入口，确认翻译没有破坏结构或运行语义。
4. 复核生成式 workspace Markdown 的生成器、旧英文标签兼容读取和中文连续更新，避免当前文件已中文化但后续生成重新回退英文。
5. 复核 Trellis 会话日志与任务归档提交主题的配置值、默认值、运行实现和临时/mocked Git 行为，确认不只修改了文档说明。
6. 复核 `AGENTS.md` 与 `.trellis/spec/workflow/documentation.md` 的长期规则，以及相关 Skill、工作流和配置之间的一致性。
7. 对原任务记录的关键验证声明执行新鲜、可重复的针对性验证，并运行与风险相称的项目质量门禁；真实失败、限制和未执行项必须保留。
8. 审计不得读取或写入真实用户 Codex/Relay 数据，不得泄漏密钥，不得修改 Git 历史，不得 push。
9. 产出独立审计报告，按严重程度列出发现，并给出原任务总体完成度结论。
10. 将默认 PRD 标题、说明和子代理 JSONL 种子说明改为简体中文；必须保留任务标题、描述、JSONL 结构、`file` / `reason` 字段、占位符和命令字面量。
11. 增加持久回归测试，覆盖 inline 任务创建、子代理 JSONL 种子、workspace 首次生成/连续更新/旧英文标签兼容，以及会话/归档自动提交中文主题和暂存范围。
12. 将 Trellis 回归测试接入项目标准检查，使 `npm run check` 能持续阻止上述行为回退。
13. 修复后更新审计报告和任务证据，只提交本任务源文件、测试和任务材料；不得纳入 `adaptive-trellis-task-policy` 的并行改动，不得 amend 或 push。
14. 会话日志自动提交只能把来源为当前精确 session 的任务纳入暂存；`session-fallback` 只可用于只读上下文恢复，不得用于任务元数据推断或 Git 暂存。

## 验收标准

- [x] 原任务全部需求和验收标准均已映射到审计矩阵，没有只依据旧勾选得出结论的项目。
- [x] 文档残留、结构、生成器、自动提交行为和长期规则均有本轮命令或文件观察证据。
- [x] 针对性测试与项目质量门禁有真实退出码和测试数量；失败与限制没有被成功重试覆盖。
- [x] 审计期间真实 `%USERPROFILE%\.codex`、`%LOCALAPPDATA%\CodexRelay` 及真实认证文件未被读取、写入或删除。
- [x] 审计报告明确列出通过、部分通过、未通过和无法验证项，并对发现提供 `file:line` 或命令证据。
- [x] 审计阶段只修改当前任务材料；用户授权修复后，差异仍严格限于设计声明的脚本、测试、检查入口、workflow spec 和当前任务材料，没有纳入真实密钥、认证文件、运行时缓存或并行任务改动。
- [x] 新建 inline 任务的默认 `prd.md` 全部使用中文标题和说明，任务标题/描述保持不变。
- [x] 子代理任务的 `implement.jsonl` / `check.jsonl` 使用中文说明，同时保持 JSONL 结构、占位符和命令不变。
- [x] workspace 与两类自动提交行为具有持久回归测试，且只使用安全临时目录或 mocked Git。
- [x] `npm run check` 包含 Trellis 回归测试并全量通过；工作提交只包含本任务路径，审计报告明确标记缺口已修复。
- [x] 会话日志提交在当前 session 指针已清除、只剩其他 session 指针时，只暂存 workspace，不读取其他任务元数据，也不提交其他任务材料。

## 范围外事项

- 不重写原任务或 Git 历史，不修改已归档任务的历史结论。
- 不修改审计发现之外的 Trellis 行为、CLI 输出或并行任务文件。
- 不运行发布构建、签名、安装、升级、卸载或人工 UI 检查，除非审计证据表明原需求实际依赖这些行为。
- 不翻译可执行源码中的普通标识符或运行时消息。
