{
  "name": "codex-relay",
  "private": true,
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:safe": "powershell -ExecutionPolicy Bypass -File scripts/prepare-dev-data.ps1",
    "dev:frontend": "vite",
    "build": "tauri build",
    "build:release": "tauri build --config src-tauri/tauri.updater.conf.json",
    "bundle:nsis": "tauri build --bundles nsis",
    "build:frontend": "vite build",
    "build:debug": "tauri build --debug --no-bundle",
    "typecheck": "vue-tsc --noEmit -p tsconfig.app.json",
    "test": "vitest run",
    "test:trellis": "python -m unittest discover -s .trellis/scripts/tests -p \"test_*.py\"",
    "test:watch": "vitest",
    "check:frontend": "npm run typecheck && npm run test",
    "check:rust": "cargo fmt --check --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings && cargo test --manifest-path src-tauri/Cargo.toml",
    "check": "npm run test:trellis && npm run check:frontend && npm run check:rust"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-autostart": "^2",
    "@tauri-apps/plugin-notification": "^2",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "vue": "^3.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/node": "^24",
    "@vitejs/plugin-vue": "^6",
    "@vue/test-utils": "^2",
    "@vue/tsconfig": "^0.8",
    "jsdom": "^26",
    "typescript": "~5.9",
    "vite": "^7",
    "vitest": "^3",
    "vue-tsc": "^3"
  }
}
